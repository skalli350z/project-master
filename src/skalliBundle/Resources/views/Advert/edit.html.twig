{% extends '::bas.html.twig' %}
{% block body %}



{{ form(form) }}
 {{form_row(form.title) }}
   {{form_row(form.author) }}
{{form_row(form.photo) }}

<a href="{{ path('skalli_list') }}">
              <INPUT TYPE="SUBMIT" VALUE=" annuler "> 

            </a>


{% endblock %}
